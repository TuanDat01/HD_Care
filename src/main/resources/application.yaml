spring:
  application:
    name: PD_project
  datasource:
    url: jdbc:mysql://localhost:3306/hdcare
    username: root
    password: 12022003b
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    banner-mode: off
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

#    lazy-initialization: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: alohcmute@gmail.com
    password: fpgi kacs tusk bbgr

    properties:
      mail:
        smtp:
          auth : true
          starttls:
            enable:  true
springdoc:
  api-docs:
    path : /api-docs
  swagger-ui:
    enabled: true
#logging:
#  level:
#    root: WARN
#    org:
#      hibernate:
#        SQL: TRACE
#        type:
#          descriptor:
#            sql:
#              BasicBinder: TRACE
#        orm:
#          jdbc:
#          bind: TRACE

server:
  port: 8082
  servlet:
    context-path: "/api/v1"
outbound:
  identity:
    client-id : ${ID}
    client_secret : ${SECRET}
    redirect-uri: ${URI}
    grant-type : ${TYPE}

jwt:
  signerKey: ${KEYJWT}

token:
  accessToken : "20"
  refreshToken : "6"
# application.yml

app:
  webhook:
    uri: http://localhost:8082/api/v1/webhook/receive

